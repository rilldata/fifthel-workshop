# Model YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/models

type: model
materialize: true

connector: duckdb

sql: |
  select * from read_json('gs://rilldata-public/db_repos/commits.json.gz', auto_detect=true, format='auto')